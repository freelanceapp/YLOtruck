<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="technology.infobite.com.yloproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ylo_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".avtivity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".avtivity.MobileRegistration" />
        <activity android:name=".avtivity.WelcomeSrceenActivity" />
        <activity android:name=".avtivity.SlidingNavigation" />
        <activity android:name=".avtivity.MyTripsActiivity" />
        <activity android:name=".avtivity.YloCashActiivity"></activity>
    </application>

</manifest>